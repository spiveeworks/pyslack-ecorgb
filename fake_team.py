channel_info = [{'name': name, 'id': id} for id, name in enumerate(
    "red blue".split()
)]

user_info = [{'name': 'player', 'id': 0} for id, name in enumerate(
    "awesome_guy boring_guy".split()
)]

